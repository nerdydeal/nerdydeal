---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const {
  title = "NerdyDeal | Weird Useful Finds, Tech Gear & Nerd Stuff Worth Buying",
  description = "NerdyDeal finds weird useful products, nerdy gear, desk gadgets, PC build ideas, DND finds, gaming accessories, anime gifts, and oddly specific stuff worth buying.",
} = Astro.props;

const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <link rel="icon" href={`${base}favicon.ico`} />
    <meta name="viewport" content="width=device-width" />

    <title>{title}</title>
    <meta name="description" content={description} />
  </head>

  <body>
    <Header />

    <slot />

    <Footer />
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      sans-serif;
    background: #0f1020;
    color: #f7f2ff;
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background:
      radial-gradient(circle at top left, rgba(255, 204, 0, 0.16), transparent 30rem),
      radial-gradient(circle at top right, rgba(112, 87, 255, 0.22), transparent 28rem),
      #0f1020;
  }

  a {
    color: inherit;
  }
</style>